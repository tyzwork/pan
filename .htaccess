Options -Indexes
ServerSignature Off

<IfModule mod_rewrite.c>
    RewriteEngine On
    # 禁止直接访问上传目录中的可执行脚本
    RewriteRule ^uploads/.*\.(php|phtml|pht|phar|asp|jsp)$ - [F,L]
</IfModule>

# 禁止访问目录列表
<FilesMatch "^\.(htaccess|htpasswd|git|svn)">
    Require all denied
</FilesMatch>
